[project]
name = "traffic-simulation"
version = "0.1.1"
description = "Experimenting with Traffic Simulation in Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fipy>=3.4.5",
    "matplotlib>=3.10.0",
    "numpy>=2.2.2",
    "scipy>=1.15.1",
]
